.unity-min-max-slider-custom__input {
    flex-direction: row;
    align-items: center;
}

.unity-min-max-slider-custom__label {
    justify-content: center;
    -unity-text-align: middle-left;
}

.unity-min-max-slider-custom__drag-container {
    flex-grow: 1;
    min-width: 20px;
}

.unity-min-max-slider-custom__text-field {
    width: 50px;
    height: 20px;
    margin-bottom: 4px;
}
